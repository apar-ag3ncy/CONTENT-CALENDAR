rules_version = '2';

// OPEN ACCESS — no login. Anyone with the app can upload/read/delete files.
// Publish in: Firebase Console -> Storage -> Rules -> Publish.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
